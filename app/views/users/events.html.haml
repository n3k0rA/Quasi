%p#notice= notice
%h2 Events created!
%div
  - if @user.created_events.empty?
    %p No events created so far..
  - else
    - @user.created_events.each do |event|
      .entry_side
        - image = image_tag(event.pic.url(:thumb), class: 'list_image')
        = link_to image, event
        %h4
          = link_to event.title , event
        %h5
          = l event.startDate, :format => :long
          | #{event.town}
        %td.list_actions
          = link_to 'Show', event
          %br
          = link_to 'Edit', edit_event_path(event)
          = link_to 'Delete', event, confirm: 'Are you sure?', method: :delete
